syntax = "proto3";

package ankrscan.namespace;
option go_package = "./;proto";

enum Namespace {
  FdbUnknown = 0;

  // ankrscan-extractor
  FdbBlockByNumberAndHash = 1;
  FdbBlockByNumber = 2;
  FdbTransactionByBlockAndIndex = 3;
  FdbTransactionByHash = 4;
  FdbExtractorConfig = 5;
  FdbConsumerOffset = 6;
}
